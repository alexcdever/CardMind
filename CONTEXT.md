# CardMind — 领域术语表

> 本文件定义产品领域的核心术语。不含实现细节，仅描述概念及其关系。
> 更新：2026-08-12

---

## 核心实体

### Note（笔记）

一个 Markdown 文档，是知识库的基本单元。每个 Note 有唯一的 UUID v7 标识。

**属性：**
- `id`：不可变，UUID v7，生成后永不改变
- `content`：Markdown 正文，可包含 `[[target-id|别名]]` 链接语法
- `tags`：标签数组（如 `["技术", "rust"]`），独立于正文存储
- `created_at`：创建时间（ISO 8601）
- `updated_at`：最后修改时间（ISO 8601）

**性质：**
- 正文是 Note 的真实信源；元数据（tags、时间戳）是 Note 的属性
- Note 不预设类型，用户通过 tags 自由分类
- 一个 Note 的生命周期：创建 → 修改 → 删除

### Link（链接）

一个 Note 对另一个 Note 的引用关系，源 Note 的正文中写入了 `[[target-id|别名]]` 语法。

**属性：**
- `source_id`：源 Note 的 ID（"谁链接了谁"）
- `target_id`：目标 Note 的 ID（"被谁链接了"）
- `alias`：链接显示文本（可选，从正文解析）

**性质：**
- 链接是源 Note 的派生数据，不是独立实体
- 链接关系通过正文解析自动维护，不手动创建
- 删除源 Note → 其所有出站链接级联删除
- 删除目标 Note → 指向它的入站链接变成悬空链接（UI 标灰）

### Tag（标签）

对 Note 的分类标记，存在于 Note 元数据中。

**性质：**
- 一个 Note 可以有多个 Tag
- Tag 是扁平结构，无层级
- Tag 是已存在的 Note 的派生数据，本身不是独立实体

---

## 操作

### 创建 Note

新 Note 生成 UUID v7，正文初始为空或给定内容，tags 默认空。

### 编辑 Note

修改正文内容。保存时：
1. 正文写入 Note 的真源
2. 解析正文中的 `[[target-id|...]]`，重建该 Note 的出站链接
3. updated_at 更新为当前时间

### 删除 Note

删除 Note 及其所有出站链接。指向该 Note 的入站链接变为悬空链接。

### 同步

多设备间的 Note 全文同步，自动后台进行，详见 `docs/sync-network.md`。同步到 Note 后，重新解析该 Note 的链接并更新本地链接索引。不跨设备实时查询链接目标是否存在——悬空链接等待同步自然解决。

### 设备（Device）

用户的一台设备（手机/电脑），运行一个 CardMind 实例。设备通过**配对**加入知识库，每台设备拥有全量副本。设备离线期间的修改在恢复连接后自动合并。

### 配对（Pairing）

两台设备建立信任关系的一次性操作。新设备显示 6 位配对码（10 分钟过期），在已信任设备上输入确认。解除配对只断开连接，不删除任何笔记。

### 回收站（Trash）

已删除笔记的 30 天缓冲。删除操作跨设备同步：A 删除 → 两端都进各自回收站；恢复操作同样传播。回收站条目支持恢复与彻底删除。

### 搜索

全文搜索 Note 的正文、标题和标签文本，不涉及元数据字段（时间戳等）。搜索结果按相关性排列。

---

## 相关关系

```
Note ──(出站链接)──→ Note
Note ──(入站链接/反链)──→ Note  （查询得出，非存储）
Note ──(拥有)────→ Tag[]

Link.source_id → Note.id
Link.target_id → Note.id
```

## 边界定义

- **个人**：所有数据属于同一用户，不同设备间同步，不存在多人协作/权限模型
- **分布式**：数据 P2P 同步，无中心服务器，不依赖云端存储；跨网段连接经 iroh relay 打洞（relay 不存储、不读取数据）。详见 `docs/sync-network.md`
- **本地优先**：所有功能离线可用，同步是增值而非必需
