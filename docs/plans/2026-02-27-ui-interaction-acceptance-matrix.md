input: UI 交互场景定义与验收维度
output: S1-S5 双轨验收矩阵与门禁口径
pos: UI 交互验收基线文档（修改需同步治理与发布门禁）
# UI 交互验收矩阵（2026-02-27）

| 场景 | 入口 | 研发轨断言 | 体验轨阈值 | 门禁 |
| --- | --- | --- | --- | --- |
| S1 引导分流 | 先本地使用 / 创建或加入池 | 点击入口后路由正确，且无空回调 | 从引导进入主工作区不超过 1 步 | 必过 |
| S2 卡片笔记管理 | 增删改查 | 新建到编辑可达；保存有状态反馈；失败可重试 | 新建并保存在目标步数阈值内，失败后可恢复 | 必过 |
| S3 池管理 | 增删改查 + 创建/加入/审批/编辑池信息/退出/解散池 | 成功/失败路径均可断言；错误码映射有下一步动作；owner 可编辑池信息并解散池 | 失败提示可理解，恢复路径可执行 | 必过 |
| S4 设置 | Tab 可一步切换至卡片页和池页 | 设置页可访问且可到池入口 | 从设置返回主任务路径可一步完成 | 必过 |
| S5 全局同步异常 | Banner/提示动作 | 异常提示可见且包含 retry/reconnect；degraded 可见且不阻断本地操作 | 同步异常不阻断本地任务，反馈时延在阈值内 | 必过 |
