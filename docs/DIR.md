目录变更需更新本文件。
项目文档与规范入口。
包含计划文档、正式规格与标准说明；权威顺序为 `standards/` > `specs/` > `plans/`，其中 `standards/` 与 `specs/` 共同约束实现落地。

plans/ - 文档目录 - 设计草案与实现计划
specs/ - 规格目录 - 面向产品级正式规格与工程落地约束的真相源文档
standards/ - 规范目录 - 项目文档标准与规则

补充说明：带 `-design.md` 命名的设计草案应优先归档到 `plans/`，不应作为正式规格长期保留在 `specs/`。
