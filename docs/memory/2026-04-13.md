# 2026-04-13 工作日志

## 今日完成

- [x] 打通 Rust 邀请串入池链路，新增 `create_pool_invite` / `join_pool_by_invite` FFI 能力，并让 `network_id` 背后绑定持久 runtime
- [x] 收口 Flutter `FrbPoolApiClient` 的 runtime handle 暴露面，业务层可只靠 `appDataDir` 完成 invite 入池
- [x] 创建池后在 Pool owner 页面暴露 invite string，先不做二维码渲染
- [x] 修复测试辅助对 macOS Rust dylib 路径的可空返回适配
- [x] 跑通本轮 Rust / Flutter 合同、单元、组件与自动化测试
- [x] 打通真实多端验证所需的调试入口：owner 自动建池、invite 导出、状态导出、iOS Rust framework 注入与 `tool/build.dart run --dart-define` 透传
- [x] 跑通 `macOS owner` 真实启动、自动建池与 invite 导出；跑通 `iOS simulator joiner` 真实启动、自动解锁、网络初始化与自动 join 触发

## 进行中

- [ ] 真实双实例联机验证（进度：93%）
  - `macOS owner` 已可真实启动、自动建池并导出 invite
  - `iOS simulator joiner` 已可真实启动到 Dart VM Service，状态文件显示 `app_lock:unlocked`、`network_ready:1`、`auto_join_triggered`
  - 当前卡点已收敛为真实 iOS join 返回 `join_error:INTERNAL`

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 12:00 | `network_id` 保留为 FFI 内部运行时句柄，不作为业务暴露概念 | 用户约束是一实例一节点一数据池，业务层不应承担 Rust 运行时对象编排 |
| 12:20 | 邀请字符串先直接以字符串形式暴露到 owner 页面，不先做二维码 | 先满足真实组网验证最短路径，避免 UI 表现层过早扩张 |
| 12:40 | `network_id` 对应持久 runtime，而不是每次 API 调用临时建 Tokio runtime | iroh endpoint 跨 runtime 复用会触发 `Internal consistency error`，必须从根上收口生命周期 |
| 15:40 | 真实双实例验证先改为 `iOS owner/macOS joiner` 与 `macOS owner/iOS joiner` 双向试探 | Android 模拟器被底层网络能力限制，iOS 模拟器更接近当前宿主网络环境 |
| 17:30 | iOS 端先采用最小合法 `cardmind_rust.framework` 注入，而不先做完整 xcframework 流程 | 目标是尽快跑通模拟器 joiner 真实启动，避免过早扩张到完整 iOS 分发策略 |

## 遇到的问题

- `flutter_rust_bridge` 内容哈希与 macOS app bundle 内 dylib 不一致
  - 处理：使用 `dart run tool/build.dart lib` 重建官方运行态 dylib，并在需要真实启动时手工覆盖到 app bundle `Frameworks/`
- Android 模拟器在 `iroh` 所需底层网络能力上被 SELinux 拒绝，无法作为稳定真实 joiner 环境
  - 处理：切换到 iOS 模拟器继续真实多端验证
- iOS 模拟器最初缺少 runtime / framework 注入，后续已补齐到可真实启动
  - 现状：当前最终卡点为 joiner 真实 `joinByCode(invite)` 返回 `INTERNAL`

## 明日计划

- [ ] 继续完成真实双实例联机验证：补充 `join_error:INTERNAL` 的 message 级观测并定位根因
- [ ] 优先尝试 `iOS owner -> macOS joiner` 反向角色路径，验证是否可绕过当前 iOS join 内部错误
- [ ] 如需继续保留 iOS 路径，补最小稳定的 iOS framework 生成/复制流程说明，避免后续本地环境重复踩坑

## 关联文档

- [数据池规格](../specs/pool.md)
- [工作流状态快照](../progress.md)
