# 2026-04-22 工作日志

## 今日完成

- [x] 在 `feat/rust-prototype-api-gap` 上完成数据池 invite 审批链路修复，并合并回 `main`
- [x] 修复 invite join 绕过审批的问题：invite 现在只能发起加入申请，必须经管理员审批后才能成为成员
- [x] 修复 invite code 冲突问题：同成员重复创建 invite 不再复用同一个 code，撤销后不会因重复 code 残留而继续生效
- [x] 修复单 pool 不变量绕过问题：在 join request 提交与审批链路补齐 endpoint 可用性校验，避免同一 endpoint 同时进入多个未解散 pool
- [x] 同步更新数据池正式规格，明确 invite、审批与单 pool 约束
- [x] 完成与本轮改动匹配的验证：`cargo fmt --check`、`cargo test pool_store_business_test -- --nocapture`、`cargo test pool_join_test -- --nocapture`、`cargo test network_flow_test -- --nocapture`、`flutter test test/unit/data/pool_api_client_test.dart test/unit/presentation/pool_controller_test.dart test/contract/api/pool_api_contract_test.dart`、`dart run tool/build.dart lib`、`flutter test test/contract/api/pool_api_contract_test.dart`
- [x] 清理开发分支与 worktree：功能分支已 merge 回 `main`，对应 worktree 已移除

## 进行中

- [ ] 暂无

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 2026-04-22 | invite 只能用于发起加入申请，不能直接让成员入池 | 用户明确确认“是否允许新成员加入”必须由管理员审核决定，invite 不应绕过正式审批链路 |
| 2026-04-22 | 数据池内任意成员都可以创建、查看并撤销 invite | 用户明确澄清 invite 管理边界不是 admin 专属，admin 的权限边界只体现在审批新成员加入 |
| 2026-04-22 | 单个 endpoint 在任一时刻只能属于一个未解散 pool，并且要在提交申请与审批阶段继续校验 | 仅在 create/join 入口校验不够，审批链路也必须守住同一业务不变量 |

## 遇到的问题

- Rust/FRB 改动后主仓出现 content hash mismatch，需执行 `dart run tool/build.dart lib` 重建运行态 dylib 后再跑 Flutter 契约测试
- 当前环境下 `git add`、`git commit`、`git worktree remove` 等写操作需要提权执行，否则会遇到索引锁或权限限制

## 明日计划

- [ ] 如继续推进数据池能力，优先做下一轮真实双端联机验证或新的 CR
- [ ] 如继续演进 invite / 审批链路，先以 `docs/specs/pool.md` 作为正式行为真相源继续收口

## 关联文档

- [数据池规格](../specs/pool.md)
- [工作流状态快照](../progress.md)
