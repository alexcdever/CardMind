# 2026-04-08 工作日志

## 今日完成

- [x] 调查 Rust 动态库“经常不存在或有问题”的根因，确认问题集中在运行态路径漂移、worktree 隔离与 FRB 绑定/动态库不同步
- [x] 完成 `docs/plans/2026-04-08-rust-dylib-runtime-path-unification-design.md` 设计文档，并通过独立审查
- [x] 完成 `docs/plans/2026-04-08-rust-dylib-runtime-path-unification-implementation-plan.md` 实施计划
- [x] 在独立 worktree 中完成 macOS 动态库运行态路径统一实现，并合并回 `main`
- [x] 将 macOS 官方运行态 dylib 收口到 `build/native/macos/libcardmind_rust.dylib`
- [x] 更新 `tool/build.dart`，使 `lib` 构建后自动同步官方运行态 dylib，`run` 改为从官方运行态目录复制到 app bundle
- [x] 新增生产侧统一入口 `lib/features/shared/runtime/rust_library_path.dart`
- [x] 将 `main.dart` 改为通过统一入口显式加载官方运行态 dylib，不再依赖开发机绝对路径或 `rust/target/release/...` 运行态回退
- [x] 将 FRB 真库测试统一收口到测试 helper，不再直接硬编码 `rust/target/release/libcardmind_rust.dylib`
- [x] 更新 `README.md`、`AGENTS.md`、`tool/DIR.md`、`lib/DIR.md`，明确 Cargo 编译缓存源与官方运行态 dylib 的职责边界

## 进行中

- [ ] 首页壳层约束收口（进度：0%）

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 2026-04-08 | macOS 运行态 dylib 的唯一真相源收口为 `build/native/macos/libcardmind_rust.dylib` | 避免测试、运行、app bundle 各自读取不同副本，降低“库不存在/库有问题”的漂移风险 |
| 2026-04-08 | `rust/target/release/libcardmind_rust.dylib` 仅保留为 Cargo 编译缓存源 | 统一“编译缓存”和“运行态产物”的职责，避免 Cargo 目录继续被误用为运行态真相源 |
| 2026-04-08 | 真库测试统一通过共享 helper 加载官方运行态 dylib | 防止各测试文件继续散落路径拼接与回退逻辑 |

## 遇到的问题

- 新 worktree 基线下，FRB 真库测试直接因找不到 `rust/target/release/libcardmind_rust.dylib` 失败；这正好验证了本轮要解决的问题不是偶发，而是当前路径策略不稳定
- `dart run tool/quality.dart flutter` 曾因一次 analyzer 提示中断；后续定位为新增测试文件里的单个 lint，修复后单独 `flutter analyze`、CLI/路径测试与真库测试均通过
- 合并后第一次并行验证把 `build lib` 与真库测试同时启动，导致测试先于官方运行态 dylib 生成而失败；改为串行执行 `dart run tool/build.dart lib && flutter test ...` 后通过

## 明日计划

- [ ] 如继续推进前端治理，先处理首页壳层职责清单落档
- [ ] 如需要继续稳固 Rust/Flutter 工程体验，再评估是否处理 worktree 间共享 Cargo 编译缓存
- [ ] 根据需要补充本次动态库路径收口的后续归档或 PR 流程

## 关联文档

- `docs/plans/2026-04-08-rust-dylib-runtime-path-unification-design.md`
- `docs/plans/2026-04-08-rust-dylib-runtime-path-unification-implementation-plan.md`
- `README.md`
- `AGENTS.md`
- `tool/build.dart`
- `docs/progress.md`
