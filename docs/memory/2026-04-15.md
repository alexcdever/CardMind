# 2026-04-15 工作日志

## 今日完成

- [x] 新增本地开发专用 `dart run tool/debug_pool.dart`，自动编排 `macOS owner -> macOS|iOS simulator joiner` 真实调试链路
- [x] 为 `tool/build.dart run` 增加隔离 macOS app 副本能力：支持复制 `cardmind.app`、改写 bundle id、ad-hoc 重签并用 `open -n` 启动
- [x] 将 macOS 调试真相源从不稳定的临时导出路径收口到隔离 bundle 对应的 `Application Support/<bundleId>/debug_invite.txt|debug_status.log`
- [x] 为 debug tool 的 macOS owner / joiner 固定独立 app 副本：`cardmind-owner.app` / `cardmind-joiner.app`
- [x] 真实复验 `dart run tool/debug_pool.dart --owner macos --joiner ios-sim` 成功，最终输出 `final status: joined:<poolId>`
- [x] 真实复验 `dart run tool/debug_pool.dart --owner macos --joiner macos` 成功，最终输出 `final status: joined:<poolId>`
- [x] 复核并确认 `2026-04-14` 的最小网络调试能力已经落地，不再重复实现 `pool_debug.invite` / `pool_debug.join.*`
- [x] 真实验证 `macOS owner -> iOS simulator joiner` 时定位到新的非网络问题：iOS 运行态 Rust framework 落后于 Dart/Rust/FRB 最新签名，导致 `join_pool_by_invite(debug_trace)` 在 FRB 解码阶段 panic
- [x] 确认根因是 `ios/Podfile` 的 `Copy Rust Framework` build phase 只复制已有 dylib，不会在 iOS 构建前自动重建 `aarch64-apple-ios-sim` / `aarch64-apple-ios` 产物
- [x] 为 iOS build phase 补自动重建 Rust dylib 逻辑：按 `SDK_NAME` 选择 target，先执行 `cargo build --release --target "$RUST_TARGET"`，再复制 framework
- [x] 新增 `test/integration/infrastructure/ios_podfile_test.dart` 锁定 `Copy Rust Framework` 必须自动重建 iOS Rust dylib
- [x] 同步更新 `ios/Runner.xcodeproj/project.pbxproj` 当前生效的 build phase，避免依赖后续再手工 `pod install`
- [x] 真实复验“无需手工预构建 iOS Rust dylib”场景：`macOS owner -> iOS simulator joiner` 再次成功 joined

## 进行中

- [ ] 跨端真实调试工具后续收口（进度：80%）
  - `dart run tool/debug_pool.dart` 已能真实跑通 `macOS -> macOS` 与 `macOS -> iOS simulator`
  - 当前剩余工作已从“链路打通”切到“是否补更多诊断信息 / 是否提交归档本轮分支”

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 12:10 | `tool/build.dart run` 必须支持隔离 app 副本、bundle id 改写与重签 | 真实 `macOS owner -> macOS joiner` 不能继续复用同一个 `cardmind.app`，否则无法形成隔离实例与独立 `appDataDir` |
| 12:40 | macOS debug tool 不再以 `/tmp` 作为 invite/status 真相源，而改读隔离 bundle 对应 `Application Support/<bundleId>` | macOS app 在真实运行时对外部临时路径写入不稳定，容器目录与既有历史验证更一致 |
| 09:20 | 不再继续追网络超时，先定位 iOS 新出现的 FRB panic 根因 | 错误已从网络层收敛为稳定的解码签名不一致，继续查网络会偏离真实问题 |
| 09:40 | 采用“iOS build phase 自动重建 Rust dylib”而不是只补文档提醒 | 手工先跑 `cargo build` 依赖人工记忆，不能从根上消除运行态二进制陈旧问题 |
| 10:10 | 同步更新 `ios/Runner.xcodeproj/project.pbxproj` 当前生效 build phase | 仅改 `Podfile` 需要后续再跑 `pod install` 才会生效，当前会话应直接让真实链路可用 |

## 遇到的问题

- 直接复用同一个 `cardmind.app` 或固定 bundle 实例会污染真实调试编排
  - 处理：改为在 `tool/build.dart run` 中复制隔离副本、重写 `CFBundleIdentifier`、重签后用 `open -n` 启动
- 仅改写隔离副本的 `Info.plist` 会导致 macOS app 无法启动
  - 处理：为隔离副本追加 `codesign --force --deep --sign - <app>` ad-hoc 重签
- `debug_pool` 最初尝试从 `/tmp` 抓取 macOS owner invite，但真实运行时该导出路径不稳定
  - 处理：改为使用隔离 bundle 对应 `Application Support/<bundleId>/debug_invite.txt` / `debug_status.log`
- `flutter run -d iPhone 16 Pro` 首次复验时，真实报错变为 FRB 解码 panic：`left: 316 right: 315`
  - 处理：核对时间戳后确认 iOS simulator 使用的 `rust/target/aarch64-apple-ios-sim/release/libcardmind_rust.dylib` 仍停留在前一天，晚于 Dart/Rust/FRB 源码签名变更
- iOS 工程现有 `Copy Rust Framework` build phase 只复制已有 dylib，不会自动重建
  - 处理：在 build phase 中按 `SDK_NAME` 选择 `RUST_TARGET`，先执行 `cargo build --release --target "$RUST_TARGET"`，再复制到 app bundle
- `flutter run` 触发后 `ios/Podfile.lock` 的 `PODFILE CHECKSUM` 自动变化
  - 现状：这是 `Podfile` 变更后的预期衍生产物，不是额外业务改动

## 明日计划

- [ ] 评估是否为 `debug_pool` 增加更可读的阶段日志与失败诊断输出
- [ ] 视需要整理并提交本轮 `debug_pool` worktree 改动
- [ ] 如继续保留 iOS 路径，评估 `Copy Rust Framework` 方案是否要升级为更稳定的 xcframework/统一产物流程
- [ ] 视需要补充容器日志读取说明，明确如何从真实 `flutter run` / app 容器拿到 invite 与 join 结果

## 关联文档

- [工作流状态快照](../progress.md)
- [最小网络互通调试方案](../plans/2026-04-14-network-diagnostic-debug-plan.md)
- [网络调试实现计划](../plans/2026-04-14-network-debug-trace-implementation.md)
- [数据池规格](../specs/pool.md)
