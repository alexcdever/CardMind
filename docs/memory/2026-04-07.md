# 2026-04-07 工作日志

## 今日完成

- [x] 完成 `docs/specs/pool.md` 的 Phase 3 行为定版
- [x] 补齐数据池规格中的应用锁前置条件、加入申请取消、解散后只读、成员退出/重入访问边界与黑盒验收标准
- [x] 运行交付前验证，确认 Flutter/Rust 主链路测试可通过，边界扫描仍以报告形式提示高优先级缺口
- [x] 澄清 `docs/` 目录权威边界，明确 `standards/` 与 `specs/` 才是当前实现依据
- [x] 将 `docs/plans/` 降权为历史设计/计划/审计记录目录，可作 ADR 参考但默认不作为当前实现依据
- [x] 创建阶段性提交：`docs: finalize pool spec for phase3 behaviors`
- [x] 创建阶段性提交：`docs: clarify document authority boundaries`

## 进行中

- [ ] 继续判断前端页面/交互层是否已成为下一批高风险负资产（进度：45%）

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 2026-04-07 | `docs/plans/` 默认不作为当前实现依据 | 计划/设计文档由 superpowers 生成，需求完成后主要保留历史追溯与 ADR 参考价值 |
| 2026-04-07 | 当前实现依据收敛为 `docs/standards/` 与 `docs/specs/` | 降低 AI 将历史计划误判为当前真相源的风险 |
| 2026-04-07 | 采用“持续开发 + 关键停顿点”而非硬阶段切分来理解协作流程 | 更贴近个人开发真实节奏，也更适合用 superpowers 作为风险控制护栏 |
| 2026-04-07 | 历史资产盘点应在每次新需求开始前执行，且最终分类由用户拍板 | 用户优先级是降低 AI 跑偏与后续维护暴雷风险 |
| 2026-04-07 | `PoolPage` 相关前端层属于可保留但不可继续扩展的高风险区域 | 状态模型、控制器与页面表达已经开始混入测试/模拟/提示拼装职责，后续再碰该区域必须先收口结构边界 |
| 2026-04-07 | 首页壳层属于可保留主壳资产，但应限制继续承担新的跨功能编排职责 | 当前导航控制器与自适应脚手架边界仍然健康，但 `AppHomepagePage` 已开始承接返回策略、退出确认、入口 guard 等编排责任，继续扩责会滑向总装入口 |

## 遇到的问题

- 初次执行 `dart run tool/quality.dart all` 时，FRB 合同测试因 Dart/Rust 内容哈希不一致失败；通过重新构建 Rust 动态库与重新生成 FRB 绑定恢复
- 重新生成 FRB 后 `rust/src/frb_generated.rs` 仅出现 import 顺序漂移；使用 `cargo fmt --all` 收敛
- 边界扫描报告仍存在高优先级缺口，但当前 `quality.dart` 仅以报告方式提示，不阻塞命令成功退出

## 明日计划

- [ ] 继续判断 App Lock 前端层是否也存在类似高风险负资产征兆
- [ ] 如确认前端层为高风险区，制定最小范围的前端资产盘点与降权策略
- [ ] 根据需要继续完善 `docs/` 入口文档与真相源边界说明

## 关联文档

- `docs/specs/pool.md`
- `docs/DIR.md`
- `docs/plans/DIR.md`
- `docs/specs/DIR.md`
- `docs/standards/spec-first-execution.md`
- `docs/progress.md`
