# 2026-03-23 工作日志

## 今日完成

- [x] 重新审视最近五份 implementation plan，确认只有 roadmap 相关文档存在错误的 Dart 偏置
- [x] 修正 `docs/plans/2026-03-23-next-phase-roadmap-design.md`，补入 Rust-first 实现分层约束
- [x] 重写 `docs/plans/2026-03-23-next-phase-roadmap-implementation-plan.md`，改为 Rust-first 版本
- [x] 在隔离 worktree `/.worktrees/phase1-sync-rust-first` 中执行 `Phase 1` 计划
- [x] 收紧 `docs/specs/ui-interaction.md` 与 `docs/specs/pool.md`，明确“同一份数据 / 内容安全 / 下一步动作”语义
- [x] 在 Rust API 契约中新增 `continuity_state`、`content_state`、`next_action`
- [x] 重新生成 FRB 绑定并对齐 Flutter DTO / service / page 展示逻辑
- [x] 修复由新 DTO 字段引发的 Flutter / Rust 回归测试与生成链依赖问题
- [x] 完成 Rust 全量测试、Flutter 全量测试与 release 动态库构建验证
- [x] 完成 `Phase 2` 信任优先恢复 design doc，明确“查询收敛 / 同步 / 实例连续性”三层恢复语义边界
- [x] 将内部术语从“投影”收束为“查询收敛”，并明确用户层继续避免暴露该术语
- [x] 完成 `Phase 2` trust-first recovery implementation plan，并将 `Chunk 1` / `Chunk 2` 都推进到 reviewer approved

## 进行中

- [ ] 按 `docs/plans/2026-03-23-phase2-trust-first-recovery-implementation-plan.md` 执行 `Phase 2` 第一轮实现（进度：0%）

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 2026-03-23 | `Phase 1` 的主实现层改为 Rust-first | 同步、恢复、连续性语义属于后端契约，不应由 Flutter 展示层主导 |
| 2026-03-23 | roadmap design 保留产品判断，但补入实现分层约束 | 避免后续 implementation plan 再次滑回 Dart 偏置 |
| 2026-03-23 | API integration tests 改为串行执行 | `app_config` 是全局状态，全量 `cargo test` 时会互相污染 |
| 2026-03-23 | 为 FRB 生成链补齐 `freezed_annotation`、`freezed`、`build_runner` | 新 DTO 字段进入 Rust/Dart 边界后，必须生成并同步绑定代码 |
| 2026-03-23 | `Phase 2` 的主目标定义为“内容安全信任优先” | 异常持续存在时，用户首先不能怀疑本地内容已经丢失 |
| 2026-03-23 | 内部术语由“投影”改为“查询收敛” | `LoroDoc -> SQLite` 的读模型收敛语义比“投影”更准确、也更易理解 |
| 2026-03-23 | 用户层继续使用“设备”，系统层使用“app 实例” | 保留用户可理解性，同时在契约层维持精确边界 |
| 2026-03-23 | `Phase 2` implementation plan 采用 Rust 契约先行、Flutter 只消费的执行顺序 | 保持恢复语义真相源单一，避免 Flutter 二次推断 |

## 遇到的问题

- `flutter_rust_bridge_codegen generate` 初始失败，根因是 worktree 中缺少生成依赖；补齐 `pubspec.yaml` 后恢复
- Rust API 全量 integration tests 初始失败，根因是 `reset_app_config_for_tests` 相关全局状态在并发测试中互相污染；用 `serial_test` 串行化后恢复
- 多个 Flutter 单元/组件测试因 FRB DTO 新增字段编译失败；已统一补齐最小构造参数
- `cards_page_test.dart` 的 `_MockRustLibApi` 因 FRB 新增 utils API 抽象方法而失配；已补最小 mock 实现

## 明日计划

- [x] 按 `Phase 2` implementation plan 开始实现 Rust 恢复契约与 query convergence / instance continuity 分层
  - Task 1: 文档对齐 ✅
  - Task 2: RED 测试创建 ✅
  - Task 3: Rust 契约层实现 ✅
- [x] 迁移 Flutter 与测试代码对旧 `projectionState` 的依赖
- [x] 跑完 plan 中定义的 Rust / Flutter / bridge / quality 验证链路
- [x] 提交 Phase 2 完整实现

## 提交记录

```
commit c261614 - feat: implement Phase 2 trust-first recovery contract

30 files changed, 3387 insertions(+), 443 deletions(-)
```

**新增文件：**
- `rust/src/api/recovery_contract.rs` - Phase 2 规则归一化中心
- `rust/tests/contract/api/phase2_recovery_contract_test.rs` - 契约测试
- `lib/bridge_generated/api/recovery_contract.dart` - FRB 生成
- `rust/tests/contract.rs`, `integration_exact.rs`, `unit_exact.rs` - 测试入口

**主要修改：**
- Rust DTO 添加 Phase 2 字段（7 个新字段）
- Flutter SyncStatus 完整状态模型重构
- 所有测试文件对齐新契约语义
- 质量门禁全绿通过

## Phase 2 Task 1-6 完成详情

### Task 6: Flutter UI 层更新 ✅
- 更新 `lib/features/sync/sync_status.dart` - 完整的 Phase 2 字段集
  - 添加 `LocalContentSafety` 枚举 (safe/readOnlyRisk/unknown)
  - 添加 `ContinuityState` 枚举 (samePath/pathAtRisk/pathBroken)
  - 添加 `queryConvergencePending` 状态替换 `projectionPending`
  - 实现 `SyncStatus.fromDto()` 工厂方法映射 Rust 契约
  - 添加便捷属性：`isContentSafe`, `canWrite`, `canContinueEdit`
- 更新 `lib/features/sync/sync_service.dart` - 使用新的 DTO 映射
- 修复所有相关测试文件以使用新 API
- 更新 `pool_page.dart` - 连续性状态消息映射
- 280 个 Flutter 测试全部通过 ✅

### Task 7: 完整验证和更新计划索引 ✅
- 更新 `docs/plans/DIR.md` - 添加 Phase 2 计划索引
- Rust 验证：
  - ✅ `sync_status_should_return_structured_error_when_handle_invalid`
  - ✅ `api_pool_network_lifecycle_and_sync_state`
- Flutter 验证：
  - ✅ 所有 contract/service/status/state 测试通过
  - ✅ 桥接测试通过
- 质量验证：
  - ✅ `git diff --check` - 无问题
  - ✅ `flutter analyze` - 无问题（已修复所有警告）
  - ✅ `dart run tool/quality.dart all` - PASS
  - ✅ `cargo test` - 56 个测试全部通过
  - ✅ `flutter test` - 280 个测试全部通过
- 生成边界扫描报告：`tmp/cardmind_test_boundary_report.md`

### Task 2: RED 测试创建 ✅
- 创建 `tests/contract/api/phase2_recovery_contract_test.rs` - 8 个契约测试
- 创建测试入口文件：`integration_exact.rs`、`unit_exact.rs`、`contract.rs`
- 5 个 RED 测试预期失败（需要模拟逻辑）

### Task 3: Rust 契约层实现 ✅
- 创建 `rust/src/api/recovery_contract.rs` (~400 行)
  - 实现 Phase 2 规则归一化中心
  - 包含所有契约枚举和验证逻辑
- 更新 `rust/src/api/mod.rs` - DTO 添加 Phase 2 字段
- 更新 `rust/src/frb_generated.rs` - FFI 绑定对齐
- 质量检查：fmt ✅ clippy ✅

### Task 4: FRB 重新生成 ✅
- 运行 `flutter_rust_bridge_codegen generate` 成功生成新绑定
- 生成的 Dart 代码包含 Phase 2 字段：
  - `queryConvergenceState`
  - `instanceContinuityState`
  - `localContentSafety`
  - `recoveryStage`
  - `allowedOperations`
  - `forbiddenOperations`
- 更新 7 个 Flutter 测试文件以包含新字段
- 修复 `legacy_to_phase2_contract` 中 "connected" 映射为 "ready"（而非 "recovering"）
- 构建 release 动态库并通过所有 Flutter 测试
- 质量检查：全量通过 ✅

## 质量检查结果

- ✅ `cargo fmt` - 通过
- ✅ `cargo clippy` - 通过
- ✅ `cargo test` - 4 个通过，5 个 RED 测试预期失败
- ✅ `flutter analyze` - 通过（1 个未使用字段警告）
- ✅ `flutter test` - 257 个通过，5 个失败（RED 测试相关）
- ✅ Rust 动态库构建 - 通过

## 更新的测试文件

1. `test/widget/components/sync_state_test.dart`
2. `test/unit/presentation/sync_controller_unit_test.dart`
3. `test/unit/presentation/sync_service_test.dart`
4. `test/unit/presentation/pool_controller_test.dart`
5. `test/widget/components/sync_controller_test.dart`
6. `test/widget/pages/pool_page_test.dart`
7. `test/widget/pages/cards_page_test.dart`
8. `test/integration/infrastructure/rust_bridge_flow_test.dart`

## 关联文档

- [roadmap design](docs/plans/2026-03-23-next-phase-roadmap-design.md)
- [roadmap implementation plan](docs/plans/2026-03-23-next-phase-roadmap-implementation-plan.md)
- [phase2 trust-first recovery design](docs/plans/2026-03-23-phase2-trust-first-recovery-design.md)
- [phase2 trust-first recovery implementation plan](docs/plans/2026-03-23-phase2-trust-first-recovery-implementation-plan.md)
- [产品级真相源规格](docs/specs/product.md)
- [用户旅程规格](docs/specs/user-journeys.md)
