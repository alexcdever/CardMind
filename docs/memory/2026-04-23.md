# 2026-04-23 工作日志

## 今日完成

- [x] 收口 Flutter / Rust 依赖升级，确认本轮升级范围包含主工程、`rust/` 主 crate、`rust/tool/boundary_scanner/` 与 FRB
- [x] 将 Flutter 侧依赖升级到当前可解析版本：`flutter_rust_bridge ^2.12.0`、`freezed_annotation ^3.1.0`、`freezed ^3.2.5`、`analyzer ^10.0.1`
- [x] 将 Rust 侧依赖升级到当前可用组合：`flutter_rust_bridge =2.12.0`、`iroh 0.98.1`、`loro 1.11.1`、`rusqlite 0.39`、`thiserror 2`、`rand_core 0.10.1`
- [x] 重新执行 `flutter_rust_bridge_codegen generate` 并同步 FRB 生成产物
- [x] 修复依赖升级后的兼容性改动，包括 `iroh` endpoint builder 用法、`argon2` 的 `OsRng` 引入位置、FRB 新增测试清理接口对应的测试桩、`analyzer` AST API 适配，以及插件生成文件更新
- [x] 同步更新技术栈基线文档中的版本说明
- [x] 完成与本轮升级匹配的验证：`cargo test`、`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo test`（`rust/tool/boundary_scanner`）、`flutter analyze`、`flutter test`
- [x] 修正 `tool/quality.dart` 的 Flutter / FRB 质量链顺序，改为先同步运行态 dylib、再执行 FRB codegen、最后跑 `flutter analyze` / `flutter test`
- [x] 为 FRB 质量链补齐 CLI 测试，覆盖新顺序与“FRB dylib 构建失败时应立即返回”的失败路径
- [x] 补齐本轮边界扫描真正缺失的 Flutter 测试：`SyncStatus` 派生语义、`FrbPoolApiClient` 缺失运行态上下文时报错、启动调试导出辅助逻辑
- [x] 将 `main.dart` 中的启动调试导出判定与日志写入提取到 `lib/app/debug_startup_support.dart`，保持行为不变并提升可测性
- [x] 收口 `tool/test_boundary_scanner.dart` 的 Flutter 高优先级出口规则，移除已验证但会误拦截的逻辑表达式/循环边界
- [x] 完成最终验证：`flutter test test/integration/infrastructure/quality_cli_test.dart`、`flutter test test/unit/presentation/sync_status_test.dart test/unit/data/frb_pool_api_client_test.dart test/unit/app/debug_startup_support_test.dart`、`flutter test test/tool/test_boundary_scanner_test.dart`、`dart run tool/quality.dart flutter`、`dart run tool/test_boundary_scanner.dart --scope=flutter`

## 进行中

- [ ] 无新增功能开发；当前仅剩提交、推送与会话存档

## 决策记录

| 时间 | 决策 | 原因 |
|------|------|------|
| 2026-04-23 | 先升级 Rust，再确认 FRB，再升级 Flutter | 用户明确要求先收口 Rust/FRB，再处理 Flutter 侧依赖 |
| 2026-04-23 | 依赖升级任务只修复由版本变化直接引出的兼容性问题，不顺带扩展业务改造 | 避免偏离本次任务边界 |
| 2026-04-23 | `quality all` 不作为本次是否完成的唯一标准，改以分层验证为准 | 仓库现有脚本会在 Flutter 测试触发 codegen 后影响 Rust `fmt --check`，这不是本次依赖升级引入的业务错误 |
| 2026-04-23 | `quality.dart` 的 Flutter 质量链必须内建 FRB 产物准备步骤 | 仓库中的 Flutter 集成/合同测试会真实加载 FRB 运行态 dylib，不能依赖“本地刚好已有旧产物” |
| 2026-04-23 | 本轮优先先补真实缺测试，再收口边界扫描器误报规则 | 先确保业务验证真实有效，再调整扫描器口径，避免把误报和真实缺口混在一起 |

## 遇到的问题

- `flutter --version` 与部分 `flutter pub` / `cargo` 操作在当前环境需要提权，因为会写本地缓存
- `dart run tool/quality.dart rust` 在 `cargo tarpaulin` 覆盖率阶段长时间无返回，因此改用 `fmt/clippy/test` 单独确认 Rust 正确性
- 扫描器曾把 Flutter 侧一些低价值逻辑表达式和路径回溯循环当成 `meaningful high priority`，已在本轮通过测试和出口规则一并收口

## 明日计划

- [ ] 如需继续提升质量基线，可继续补齐 `tmp/cardmind_test_boundary_report.md` 中尚未转化为出口阻塞的覆盖率缺口
- [ ] 如需继续收口质量脚本，可评估是否让 `dart run tool/quality.dart all` 在全链路场景下也纳入更稳定的 Rust 覆盖率策略

## 关联文档

- [工作流状态快照](../progress.md)
- [技术栈基线](../standards/tech-stack-baseline.md)
- [边界扫描报告](../../tmp/cardmind_test_boundary_report.md)
