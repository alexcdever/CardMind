import 'package:flutter/material.dart';

/// 节点管理页面
class NodeManagementScreen extends StatelessWidget {
  const NodeManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('节点管理'),
      ),
      body: const Center(
        child: Text('节点管理页面内容待实现'),
      ),
    );
  }
}
