# 目录约定

## 概述

本文档描述 CardMind OpenSpec 文档的目录结构和命名约定。

**迁移日期**：2026-01-20
**结构版本**：1.0.0（领域驱动）

---

## 目录结构

```
openspec/specs/
├── domain/            # 领域模型和业务逻辑
├── api/               # 公共 API 和 FFI 接口
├── features/          # 面向用户的功能
└── ui_system/         # 设计令牌和共享 UI 组件
```

---

## 顶层目录

### 🏗️ `domain/`

**目的**：系统做什么

**包含**：领域模型、核心类型、业务逻辑规格

**示例文件**：
- `common_types.md` - 共享类型（UUID、时间戳）
- `pool_model.md` - 单池架构
- `card_store.md` - 卡片存储逻辑
- `sync_protocol.md` - 同步算法
- `device_config.md` - 设备配置

**命名**：`snake_case.md`

---

### 🔌 `api/`

**目的**：组件如何通信

**包含**：公共 API、FFI 接口、外部契约

**示例文件**：
- `api_spec.md` - 主要 Rust API 规格

**命名**：`snake_case.md`

---

### ✨ `features/`

**目的**：用户可以做什么

**包含**：按功能组织的面向用户的功能

**结构**：
```
features/
├── card_editor/
│   ├── logic.md          # Rust/后端逻辑（可选）
│   ├── ui_mobile.md      # Flutter 移动端 UI
│   ├── ui_desktop.md     # Flutter 桌面端 UI
│   └── ui_shared.md      # 共享 UI 逻辑（可选）
├── card_list/
│   └── ...
└── ...
```

**命名约定**：
- 功能目录：`lowercase_with_underscores`（小写下划线）
- 逻辑文件：`logic.md`（Rust/后端）
- UI 文件：`ui_mobile.md`、`ui_desktop.md`、`ui_shared.md`

**示例功能**：
- `card_editor/` - 卡片编辑界面
- `card_list/` - 卡片列表/网格视图
- `search/` - 搜索功能
- `onboarding/` - 首次用户体验
- `sync_feedback/` - 同步状态 UI

**规则**：
- ✅ 按用户功能组织，而非按技术栈
- ✅ 使用描述性名称（card_editor，而非 editor）
- ✅ 当 UI 不同时按平台拆分（ui_mobile.md vs ui_desktop.md）
- ✅ 当 UI 在各平台相同时使用 ui_shared.md
- ❌ 不要使用技术栈前缀（SP-FLT-MOB-001）

---

### 🎨 `ui_system/`

**目的**：一致的 UI 基础

**包含**：设计令牌、布局系统、共享组件

**示例文件**：
- `design_tokens.md` - 颜色、排版、间距
- `responsive_layout.md` - 响应式布局系统
- `shared_widgets.md` - 可复用 UI 组件

**命名**：`snake_case.md`

---

## 文件命名约定

### 工程文档
- **格式**：`snake_case.md`
- **语言**：中文
- **编码**：UTF-8
- **行尾**：Unix (LF)，而非 Windows (CRLF)

### 功能文件
- 后端逻辑：`logic.md`
- 移动端 UI：`ui_mobile.md`
- 桌面端 UI：`ui_desktop.md`
- 共享 UI：`ui_shared.md`

### 规格文件
- 领域规格：描述性名称（例如：`card_store.md`、`sync_protocol.md`）
- 文件名中不使用技术栈前缀

---

## 从旧结构迁移

### 旧结构（已废弃）
```
openspec/specs/
├── rust/           # ❌ 已废弃 → 已迁移到 domain/ 和 api/
└── flutter/        # ❌ 已废弃 → 已迁移到 features/ 和 ui_system/
```

### 迁移映射

| 旧路径 | 新路径 | 说明 |
|----------|----------|------|
| `rust/api_spec.md` | `api/api_spec.md` | API 规格 |
| `rust/card_store_spec.md` | `domain/card_store.md` | 卡片存储 |
| `rust/sync_spec.md` | `domain/sync_protocol.md` | 同步协议 |
| `flutter/mobile/SP-FLT-MOB-002-card-editor.md` | `features/card_editor/ui_mobile.md` | 卡片编辑器 |
| `flutter/desktop/SP-FLT-DSK-001-card-grid.md` | `features/card_list/ui_desktop.md` | 卡片列表 |

---

## 验证

### 目录结构
```bash
# 验证所有顶层目录存在
ls -d openspec/specs/{engineering,domain,api,features,ui_system,adr}

# 统计功能目录数量（应为 11）
ls -d openspec/specs/features/*/ | wc -l
```

### 文件迁移
```bash
# 统计新结构中的文件数量
find openspec/specs/{engineering,domain,api,features,ui_system} -name "*.md" | wc -l

# 验证 features/ 中没有技术栈前缀
grep -r "SP-FLT-" openspec/specs/features/
```

---

## 最佳实践

### ✅ 应该做
- 按用户功能组织（features/）
- 使用描述性的、以领域为中心的名称
- 仅在 UI 不同时按平台拆分
- 保持后端逻辑独立（logic.md）

### ❌ 不应该做
- 按技术栈组织（顶层不使用 rust/、flutter/）
- 使用技术栈前缀（SP-FLT-MOB-001）
- 在一个文件中混合平台特定和共享 UI
- 为内部工具创建功能目录

---

## 参考

- 配置：`openspec/.openspec/config.json`
- 规格编码指南：`openspec/engineering/guide.md`
- ADR 索引：`docs/adr/`（架构决策记录现在位于 docs/ 目录）
