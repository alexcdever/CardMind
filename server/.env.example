# 部署模式: 'desktop' 或 'web'
DEPLOYMENT_MODE=web

# PostgreSQL 连接 URL (仅在 web 模式下需要)
DATABASE_URL=postgres://user:password@localhost:5432/cardmind

# 服务器配置
PORT=8000
HOST=0.0.0.0

# CORS 配置
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:8080,https://cardmind.example.com
