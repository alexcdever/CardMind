# 服务器配置
SERVER_HOST=127.0.0.1
SERVER_PORT=9000

# 数据目录
DATA_DIR=./data

# 部署模式 (local 或 center)
DEPLOYMENT_MODE=local

# PostgreSQL连接URL（仅在中心服务器模式下需要）
# POSTGRES_URL=postgres://user:password@localhost:5432/cardmind
